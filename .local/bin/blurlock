#!/bin/bash
# Requires ./screenshot, imagemagick, and swaylock
# Takes a screenshot, blurs it, and then locks the screen
#  with the blurred image.

# take screenshot
screenshot fullscreen /tmp/screenshot.png

# blur it
magick /tmp/screenshot.png -blur 0x5 /tmp/screenshotblur.png
rm /tmp/screenshot.png

# lock the screen
swaylock -f -i /tmp/screenshotblur.png

exit 0
